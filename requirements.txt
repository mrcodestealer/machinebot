# Core Lark bot (persistent connection + webhook)
lark-oapi>=1.2.0
flask>=3.0
requests>=2.31
python-dotenv>=1.0
pycryptodome>=3.20            # only needed if the Lark console Encrypt Key is enabled

# One-time maintenance / stress-test reminders (reminder.py + scheduler in main.py)
apscheduler>=3.10

# Machine engines: prod-batch set/unset (smmachine / prod_machine_batch), webmachine
# scrape loop, OSM-Watch warm browser + encoder scraper (osmwatch), checkcredit /
# checkmachinelog Third-Http screenshots
playwright>=1.44              # then: playwright install chromium
pillow>=10.1                  # error-context / EGM screenshot rendering. 10.1 is the floor
                              # on purpose: ImageFont.load_default(size=...) arrived there,
                              # and it is what keeps /machineerror log screenshots sharp on
                              # a host with no system fonts (10.0 falls back to an 11px
                              # bitmap that ignores the requested size).

# Amount Loss / CHECKLOG (/al) — Aliyun SLS logs + FPMS TOTP login
pyotp>=2.9
aliyun-log-python-sdk>=0.9
aliyun-python-sdk-core>=2.15
aliyun-python-sdk-sts>=3.1

# Optional AI log summary (checkmachinelog → chatagent). Omit on CPU-only hosts;
# the summary degrades gracefully when the LLM backend is unreachable.
# torch
# transformers
